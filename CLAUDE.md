# CLAUDE.md

## プロジェクト概要
HTML / CSS / JavaScript のみで構成するプロジェクト。フレームワークやビルドツールは使用しない。

## ルール
- CSSは `css/style.css` にまとめる。ファイルを分割しない。
- JavaScriptは `js/script.js` にまとめる。ファイルを分割しない。
- 配色は `css/style.css` の `:root` に定義済みのトークンのみを使う。新しい色を追加しない。
- 装飾は最小限にし、影・グラデーションの多用を避ける。
- セマンティックなHTML構造を作成する。BEM記法を用い、header/main/footerの役割を明確にすること。
- コメントは日本語で書く。
- 動作確認用の `console.log` は残さない。コミット・完了報告の前に必ず削除する。
